services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: calmpoint
      POSTGRES_PASSWORD: calmpoint
      POSTGRES_DB: calmpoint
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
